'use strict';

var tslib_es6 = require('../../node_modules/tslib/tslib.es6.js');
var React = require('react');
var PropTypes = require('prop-types');
var index = require('../../_virtual/index.js');
var props = require('../../props.js');

const CSpinner = React.forwardRef((_a, ref) => {
    var { as: Component = 'div', className, color, size, variant = 'border', visuallyHiddenLabel = 'Loading...' } = _a, rest = tslib_es6.__rest(_a, ["as", "className", "color", "size", "variant", "visuallyHiddenLabel"]);
    return (React.createElement(Component, Object.assign({ className: index.default(`spinner-${variant}`, {
            [`spinner-${variant}-${size}`]: size,
            [`text-${color}`]: color,
        }, className), role: "status" }, rest, { ref: ref }),
        React.createElement("span", { className: "visually-hidden" }, visuallyHiddenLabel)));
});
CSpinner.propTypes = {
    as: PropTypes.string,
    className: PropTypes.string,
    color: props.colorPropType,
    size: PropTypes.oneOf(['sm']),
    variant: PropTypes.oneOf(['border', 'grow']),
    visuallyHiddenLabel: PropTypes.string,
};
CSpinner.displayName = 'CSpinner';

exports.CSpinner = CSpinner;
//# sourceMappingURL=CSpinner.js.map
