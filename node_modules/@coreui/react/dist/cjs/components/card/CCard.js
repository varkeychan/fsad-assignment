'use strict';

var tslib_es6 = require('../../node_modules/tslib/tslib.es6.js');
var React = require('react');
var PropTypes = require('prop-types');
var index = require('../../_virtual/index.js');
var props = require('../../props.js');

const CCard = React.forwardRef((_a, ref) => {
    var { children, className, color, textBgColor, textColor } = _a, rest = tslib_es6.__rest(_a, ["children", "className", "color", "textBgColor", "textColor"]);
    return (React.createElement("div", Object.assign({ className: index.default('card', {
            [`bg-${color}`]: color,
            [`text-${textColor}`]: textColor,
            [`text-bg-${textBgColor}`]: textBgColor,
        }, className) }, rest, { ref: ref }), children));
});
CCard.propTypes = {
    children: PropTypes.node,
    className: PropTypes.string,
    color: props.colorPropType,
    textBgColor: props.colorPropType,
    textColor: PropTypes.string,
};
CCard.displayName = 'CCard';

exports.CCard = CCard;
//# sourceMappingURL=CCard.js.map
