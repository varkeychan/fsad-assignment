'use strict';

var tslib_es6 = require('../../node_modules/tslib/tslib.es6.js');
var React = require('react');
var PropTypes = require('prop-types');
var index = require('../../_virtual/index.js');
var CCloseButton = require('../close-button/CCloseButton.js');
var CModalContext = require('./CModalContext.js');

const CModalHeader = React.forwardRef((_a, ref) => {
    var { children, className, closeButton = true } = _a, rest = tslib_es6.__rest(_a, ["children", "className", "closeButton"]);
    const { setVisible } = React.useContext(CModalContext.CModalContext);
    return (React.createElement("div", Object.assign({ className: index.default('modal-header', className) }, rest, { ref: ref }),
        children,
        closeButton && React.createElement(CCloseButton.CCloseButton, { onClick: () => setVisible(false) })));
});
CModalHeader.propTypes = {
    children: PropTypes.node,
    className: PropTypes.string,
    closeButton: PropTypes.bool,
};
CModalHeader.displayName = 'CModalHeader';

exports.CModalHeader = CModalHeader;
//# sourceMappingURL=CModalHeader.js.map
