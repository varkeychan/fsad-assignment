'use strict';

var tslib_es6 = require('../../node_modules/tslib/tslib.es6.js');
var React = require('react');
var PropTypes = require('prop-types');
var index = require('../../_virtual/index.js');
var useForkedRef = require('../../hooks/useForkedRef.js');
require('@popperjs/core');
var Transition = require('../../node_modules/react-transition-group/esm/Transition.js');

const CBackdrop = React.forwardRef((_a, ref) => {
    var { className = 'modal-backdrop', visible } = _a, rest = tslib_es6.__rest(_a, ["className", "visible"]);
    const backdropRef = React.useRef(null);
    const forkedRef = useForkedRef.useForkedRef(ref, backdropRef);
    return (React.createElement(Transition.default, { in: visible, mountOnEnter: true, nodeRef: backdropRef, timeout: 150, unmountOnExit: true }, (state) => (React.createElement("div", Object.assign({ className: index.default(className, 'fade', {
            show: state === 'entered',
        }) }, rest, { ref: forkedRef })))));
});
CBackdrop.propTypes = {
    className: PropTypes.string,
    visible: PropTypes.bool,
};
CBackdrop.displayName = 'CBackdrop';

exports.CBackdrop = CBackdrop;
//# sourceMappingURL=CBackdrop.js.map
