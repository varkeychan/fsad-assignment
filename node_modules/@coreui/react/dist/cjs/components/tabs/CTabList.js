'use strict';

var tslib_es6 = require('../../node_modules/tslib/tslib.es6.js');
var React = require('react');
var PropTypes = require('prop-types');
var index = require('../../_virtual/index.js');
var useForkedRef = require('../../hooks/useForkedRef.js');
require('@popperjs/core');
var getNextActiveElement = require('../../utils/getNextActiveElement.js');

const CTabList = React.forwardRef((_a, ref) => {
    var { children, className, layout, variant } = _a, rest = tslib_es6.__rest(_a, ["children", "className", "layout", "variant"]);
    const tabListRef = React.useRef(null);
    const forkedRef = useForkedRef.useForkedRef(ref, tabListRef);
    const handleKeydown = (event) => {
        if (tabListRef.current !== null &&
            (event.key === 'ArrowDown' ||
                event.key === 'ArrowUp' ||
                event.key === 'ArrowLeft' ||
                event.key === 'ArrowRight' ||
                event.key === 'Home' ||
                event.key === 'End')) {
            event.preventDefault();
            const target = event.target;
            // eslint-disable-next-line unicorn/prefer-spread
            const items = Array.from(tabListRef.current.querySelectorAll('.nav-link:not(.disabled):not(:disabled)'));
            let nextActiveElement;
            if (event.key === 'Home' || event.key === 'End') {
                nextActiveElement = event.key === 'End' ? items.at(-1) : items[0];
            }
            else {
                nextActiveElement = getNextActiveElement.default(items, target, event.key === 'ArrowDown' || event.key === 'ArrowRight', true);
            }
            if (nextActiveElement) {
                nextActiveElement.focus({ preventScroll: true });
            }
        }
    };
    return (React.createElement("div", Object.assign({ className: index.default('nav', variant === 'enclosed-pills' && 'nav-enclosed', // Enclosed pills variant required for `.nav-enclosed` class
        {
            [`nav-${layout}`]: layout,
            [`nav-${variant}`]: variant,
        }, className), role: "tablist", onKeyDown: handleKeydown, ref: forkedRef }, rest), children));
});
CTabList.propTypes = {
    children: PropTypes.node,
    className: PropTypes.string,
    layout: PropTypes.oneOf(['fill', 'justified']),
    variant: PropTypes.oneOf([
        'enclosed',
        'enclosed-pills',
        'pills',
        'tabs',
        'underline',
        'underline-border',
    ]),
};
CTabList.displayName = 'CTabList';

exports.CTabList = CTabList;
//# sourceMappingURL=CTabList.js.map
